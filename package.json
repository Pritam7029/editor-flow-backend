{
  "name": "editorflow-backend-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root package runner for EditorFlow backend",
  "workspaces": [
    "server"
  ],
  "scripts": {
    "build": "npm --prefix server install",
    "start": "npm --prefix server start"
  }
}
